/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["demos.creative-tim.com","https://images.unsplash.com"],
    },
    reactStrictMode: false,
  };
  
  export default nextConfig;